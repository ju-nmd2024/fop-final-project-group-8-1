// export default class UpdateScore {
//     let points = Math.floor(50*Math.random());

//     if (velocity < 0) {
//         maxScore += points;
//         if (score < maxScore) {
//             score = maxScore;
//         }
//     } else if (velocity >= 0) {
//         maxScore -= points;
//     }
// }
